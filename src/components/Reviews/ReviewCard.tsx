import {
    Card,
    Typography,
    CardMedia,
    CardActionArea,
    CardContent,
} from '@mui/material';

import { LensReview } from '../../types/types.ts';

const ReviewCard = (lensReview: LensReview) => {
    return;
    <></>;
};

export default ReviewCard;
