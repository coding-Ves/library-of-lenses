import { Typography } from '@mui/material';

const CreateReview = () => {
    return (
        <>
            <Typography>Review Creation page</Typography>
        </>
    );
};

export default CreateReview;
