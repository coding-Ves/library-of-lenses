import { Typography } from '@mui/material';

const Home = () => {
    return (
        <>
            {/* Include top nav bar + outlet */}
            <Typography variant='h1'>Home</Typography>
        </>
    );
};

export default Home;
