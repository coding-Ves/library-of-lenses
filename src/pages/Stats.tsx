import { Typography } from '@mui/material';

const Stats = () => {
    return (
        <>
            {/* Displays stats for users, activity of posts etc. */}
            <Typography>Stats</Typography>
        </>
    );
};

export default Stats;
